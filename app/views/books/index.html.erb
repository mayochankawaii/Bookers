<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="homes.scss" />
    <title>Books</title>
  </head>

  <body>

  <h1>Books</h1>

  <% @books.each do |book| %>
  
    <p>タイトル</p>  
    <span><%= book.title %></span>

  <% end %>
  
<% if @book.errors.any? %>
  <%= @book.errors.count %>件のエラーが発生しました
  <ul>
    <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<!-- form_with部分 -->
<%= form_with model: @book do |f| %>

<div class="field"> 
  <h4>タイトル</h4>
  <%= f.text_field :title %>
</div>
<div class="field"> 
  <h4>本文</h4>
  <%= f.text_area :body %>
</div>

  <Div Align="left"><%= f.submit '投稿' %></Div>

<% end %>

  </body>
</html>